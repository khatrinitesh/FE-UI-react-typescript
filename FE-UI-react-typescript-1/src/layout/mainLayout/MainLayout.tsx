import type { I_LayoutProps } from "../../interface";

const MainLayout = ({ children }: I_LayoutProps) => {
  return <div className="appLayou w-full">
    <Header
  </div>;
};

export default MainLayout;
