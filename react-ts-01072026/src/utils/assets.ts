const ASSET_URL = "/assets"

export const IMAGES = {
  logo1: `${ASSET_URL}/img/logo1.png`,
  logo2: `${ASSET_URL}/img/logo2.png`,
}

export const VIDEOS = {
  intro: `${ASSET_URL}/videos/intro.mp4`,
}

export const LOTTIE = {
  loader: `${ASSET_URL}/lottie/loader.json`,
}

export const PDFS = {
  brochure: `${ASSET_URL}/pdfs/brochure.pdf`,
}