import type { VerticalLineConfig } from "../../interface";

export const VERTICAL_LINE_CONSTANTS: VerticalLineConfig = {
  color: "#4ade80", // green-400
  height: 200,
  thickness: 4,
  visible: true,
};
