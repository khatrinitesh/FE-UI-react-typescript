import type { TransparentTextSettings } from "../../interface";

export const DEFAULT_TRANSPARENT_TEXT_SETTINGS: TransparentTextSettings = {
  text: "Sample Text",
  opacity: 0.5,
  color: "#ffffff",
};
