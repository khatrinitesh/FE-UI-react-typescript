import type { HoverTransitionSettings } from "../../interface";

export const DEFAULT_HOVER_TRANSITION_SETTINGS: HoverTransitionSettings = {
  scale: 1.1,
  duration: 0.3,
  color: "#3b82f6", // Tailwind blue-500
};
