const ASSET_URL = "/assets"

export const IMAGES = {
  logo1: `${ASSET_URL}/img/logo1.png`,
  logo2: `${ASSET_URL}/img/logo2.png`,
  hero: `${ASSET_URL}/img/hero.png`,
  about: `${ASSET_URL}/img/about.png`,
} as const

export const VIDEOS = {
  intro: `${ASSET_URL}/video/intro.mp4`,
} as const

export const LOTTIES = {
  loading: `${ASSET_URL}/lottie/loading.json`,
} as const

export const PDFS = {
  brochure: `${ASSET_URL}/pdf/brochure.pdf`,
} as const