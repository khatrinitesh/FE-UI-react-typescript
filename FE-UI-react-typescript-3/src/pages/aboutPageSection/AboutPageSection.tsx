const AboutPageSection = () => {
  const a = 10;
  const b = 20;
  const sum = a + b;

  return (
    <div className="innerContent">
      <div className="container mx-auto">About - {sum}</div>
    </div>
  );
};

export default AboutPageSection;
